name: firebase_auth_bug
description: Minimal repro for Firebase Auth web multi-tab logout (flutterfire).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.11.1 <4.0.0"
  flutter: ">=3.41.0 <3.42.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
